<p>Here's the image:</p>
<%= link_to "Back", images_path %>
<%= link_to "Delete", @image, :method => "delete" %>
<p><%= @image.title %></p>
<%= link_to image_tag(@image.url), edit_image_path(@image) %>
